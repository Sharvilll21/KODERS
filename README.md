# ML Framework Project

This is a modular machine learning framework built from scratch.  
It includes data preprocessing, model training, evaluation, and pipeline components.

## How to run
Install dependencies:
```bash
pip install -e .